// Array is the collection of elements of the same data type stored in contiguous memory locations.
// Arrays are used to store multiple values of the same data type in a single variable.

let ages = [20, 21, 19, 18, 22];

for (let i = 0; i < ages.length; i++) {
  console.log(ages[i]);
}
